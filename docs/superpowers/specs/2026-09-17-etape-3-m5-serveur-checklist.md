# Étape 3 — M5 Serveur : MCP streamable HTTP, hub WebSocket, phases, journal, replay — Checklist

Issue : #5 · Plan : `docs/superpowers/plans/2026-09-17-etape-3-m5-serveur.md` · Contrat : `docs/superpowers/plans/2026-09-17-etape-3-architecture.md` · Spec : sections 3, 5, 8, 9.

Tout le code M5 est dans `packages/server/**` (hors `packages/server/src/agent/**` et `packages/server/prompts/`, réservés à M6) et `packages/sim/src/bridge/` ; hors de ces dossiers, seuls `packages/sim/src/App.tsx` (création du bridge, `window.__tomato.bridge`) et le `package.json` racine (`dev:server`) sont touchés. `packages/shared` n'est pas modifié.

## Code
- [ ] [SPEC-1] Dépendances épinglées et vérifiées (`@modelcontextprotocol/sdk` 1.30.0, `express` 5.2.1, `ws` 8.21.3, `tsx` 4.23.13, types), scripts `dev` = `tsx watch src/index.ts`, `start` = `tsx src/index.ts`, `typecheck`/`build` = `tsc --noEmit`, racine `dev:server` ; `VERSION` dans `src/version.ts` — `packages/server/package.json`, `package.json`, `packages/server/src/version.ts`
- [ ] [SPEC-2] Configuration par variables d'environnement `TOMATO_MCP_PORT` (7331), `TOMATO_WS_PORT` (7332), `TOMATO_MODEL` (`claude-opus-5`), `TOMATO_AGENT` (`on`/`off`), `TOMATO_EPISODES_DIR` (`data/episodes`), valeurs invalides → défaut — `packages/server/src/config.ts`
- [ ] [SPEC-3] Règles de phase pures : premier outil de mouvement en `detected` → `harvesting` ; `cut` réussi → `cutting` puis `falling` ; `tomato_landed` en `falling` → `harvested`/`missed` ; clôture → `idle` (via `aborted` si l'épisode n'est pas fini), impossible pendant `falling` ; issue réelle déduite de la phase — `packages/server/src/state/rules.ts`
- [ ] [SPEC-4] Journal : un fichier `data/episodes/<episodeId>.json` par épisode `{ episodeId, tomatoId, startedAt, endedAt, outcome, note, messages: [{ atMs, message }], toolCalls, costUsd }`, images des `views` conservées, liste `{ episodeId, startedAt, outcome, tomatoId }`, lecture par id (id validé), `costUsd` mis à jour par un `episode_end` tardif — `packages/server/src/episodes/journal.ts`
- [ ] [SPEC-5] `createHub(port) → Hub { onSimMessage, sendToSim, broadcast, simConnected, close }` (+ `onBroadcast`, `setSnapshot`, `whenListening`) : `hello` obligatoire, un seul client `sim` (le nouveau remplace et ferme l'ancien), `snapshot` envoyé après le `hello`, `ServerToDashboard` diffusé à TOUS les clients, seuls `state`/`sim_event`/`action_result`/`views_result` du client sim sont routés, messages illisibles ou hors rôle ignorés et journalisés, `parseMessage` de shared — `packages/server/src/hub/hub.ts`
- [ ] [SPEC-6] `createSimBridge(hub) → SimBridge { apply, renderViews, latestState, onEvent }` : corrélation par `requestId` (`crypto.randomUUID()`), délai 15 s (`SIM_TIMEOUT_MS`) et absence de sim → `fail(latestState ?? createDefaultWorld(0), 'not_available', 'simulation did not answer')` pour `apply`, résultat sans image pour `renderViews` ; dernier `state` mémorisé — `packages/server/src/sim/simBridge.ts`, `packages/server/src/sim/viewsFallback.ts`
- [ ] [SPEC-7] `createSession(hub, deps) → Session { get, setPhase, startEpisode, endEpisode, onPhase }` (+ `handleSimEvent`, `noteToolCall`, `noteToolResult`, `onWake`, `pendingDetections`) : `SessionState { phase, episodeId, targetTomatoId, lastEvent, harvested, missed, toolCallsThisEpisode }`, `setPhase` via `transition` de shared (transition invalide journalisée et ignorée, jamais lancée), chaque changement diffuse `phase` et `block_activity` ; `ripe_detected` en `idle` → épisode (`detected`, `set_target` vers la sim, réveil), hors `idle` → file d'attente rejouée au retour en `idle`, `plant_regenerated` vide la file ; clôture différée pendant `falling` ; `episodeId` horodaté sûr pour un nom de fichier — `packages/server/src/state/session.ts`
- [ ] [SPEC-8] Formatage pur : blocs texte/image, nombres en français (`fr`), en-tête « Vue front — axes X→ Z↑ — 8 px/cm », `actionResultText` (`ok : …` + JSON ciblé, ou `code : message (détails)`), résumés d'une ligne (« ciseaux vers X 12, Y 4, Z 38 », « coupe : misaligned, 1,4 cm, 62° ») — `packages/server/src/mcp/format.ts`
- [ ] [SPEC-9] Handlers des neuf outils avec `ToolSchemas` (`safeParse` → texte `invalid_argument : …`) : `get_status` JSON compact (phase de session, épisode, cible, sim connectée, compteur, dernier événement, tomates, poses, limites) ; `get_views` → par caméra en-tête texte + image PNG base64 puis JSON `ViewsPayload` (phase du serveur), diffusion `views` avec `episodeId` ; `move_camera` → texte + vue rafraîchie de la caméra ; `move_scissors`/`rotate_scissors`/`open_scissors`/`cut`/`move_basket` → texte `ActionResult` ; `report` → accusé, clôture après diffusion du résultat (`after`), refus texte pendant `falling`, inoffensif sans épisode ; aucune exception — `packages/server/src/mcp/handlers.ts`
- [ ] [SPEC-10] Enveloppe commune : `tool_call_start { episodeId: session.episodeId ?? 'manual', callId, tool, args }` → compteur → garde `MAX_TOOL_CALLS_PER_EPISODE` (tout outil sauf `report` reçoit « limite atteinte …, appelle report ») → handler (exception interne → texte `not_available`) → règles de phase → `tool_call_result { ok, summary, durationMs }`, `block_activity` agent↔serveur ; `createMcpServer(deps) → McpServer` « tomato-robot » avec `registerTool` × 9, descriptions `TOOL_DESCRIPTIONS`, `isError` = `!ok` — `packages/server/src/mcp/toolRunner.ts`, `packages/server/src/mcp/createMcpServer.ts`
- [ ] [SPEC-11] Express : `POST /mcp` streamable HTTP en mode sans session (transport et serveur neufs par requête, fermés à la fin de la réponse), `GET`/`DELETE /mcp` → 405, `GET /health`, `GET /episodes`, `GET /episodes/:id` (404 si absent) — `packages/server/src/http/app.ts`
- [ ] [SPEC-12] Crochet `AgentRunner { wake, busy, stop }`, `AgentRunnerDeps { hub, session, mcpUrl, model, systemPrompt }`, runner inerte, chargement dynamique de `src/agent/index.ts` (M6) avec repli ; `index.ts` câble hub, bridge, journal, session, Express sur `127.0.0.1`, runner (inerte si `TOMATO_AGENT=off` ou M6 absent), `session.onWake → runner.wake`, arrêt propre — `packages/server/src/agentRunner.ts`, `packages/server/src/index.ts`
- [ ] [SPEC-13] `createBridge({ url, runtime, renderViews }) → Bridge { onServerMessage, status, onStatus, close }` : `hello { role: 'sim' }` puis `state` à l'ouverture, `state` à chaque changement du store limité à 5 Hz (dernier état conservé) et immédiatement après chaque `action_result`, `apply_action` → `runtime.apply` → `action_result`, `render_views` → `views_result` (sans image si le rendu échoue), `runtime.onEvent` → `sim_event`, messages dashboard republiés, reconnexion toutes les 2 s, socket injectable — `packages/sim/src/bridge/bridge.ts`, `packages/sim/src/bridge/viewsFallback.ts`
- [ ] [SPEC-14] `createFakeBridge(script: { atMs, message }[], speed = 1) → Bridge` : rejoue le scénario dans l'ordre des `atMs` (divisés par `speed`), connecté, `close()` annule — `packages/sim/src/bridge/fakeBridge.ts`
- [ ] [SPEC-15] `App.tsx` crée le bridge après `createRuntime` (`ws://localhost:7332`, `renderViews` lu dans `window.__tomato` à chaque demande), expose `window.__tomato.bridge`, le ferme au démontage — `packages/sim/src/App.tsx`

## Tests
- [ ] [TEST-2] `packages/server/src/config.test.ts` — défauts documentés ; variables lues ; port invalide → défaut
- [ ] [TEST-3] `packages/server/src/state/rules.test.ts` — mouvement en detected → harvesting ; cut ok → cutting, falling ; atterrissage seulement en falling ; issue depuis la phase ; phases de clôture
- [ ] [TEST-4] `packages/server/src/episodes/journal.test.ts` — fichier écrit avec `atMs`, relecture, liste, id non sûr refusé, patch du coût, dossier absent
- [ ] [TEST-5] `packages/server/src/hub/hub.test.ts` (vrai `ws`, port éphémère) — snapshot après hello ; routage des messages sim seulement ; messages illisibles/hors rôle journalisés ; broadcast à tous ; sendToSim vers la sim seule et false sans sim ; remplacement du client sim
- [ ] [TEST-6] `packages/server/src/sim/simBridge.test.ts` (faux hub) — corrélation par requestId dans le désordre ; dernier état ; views_result ; sans sim → not_available immédiat ; délai dépassé → not_available ; réponse orpheline journalisée ; relais des événements
- [ ] [TEST-7] `packages/server/src/state/session.test.ts` — épisode ouvert par ripe_detected (detected, set_target, réveil, journal, diffusions) ; chemin nominal jusqu'à idle ; missed et issue réelle vs déclarée ; abandon ; file d'attente rejouée et vidée par new_plant ; transitions invalides ignorées, clôture différée pendant falling, compteur nul hors épisode
- [ ] [TEST-8] `packages/server/src/mcp/format.test.ts` — nombres français ; en-tête de vue ; détails ; textes ok/erreur ; résumés
- [ ] [TEST-9] `packages/server/src/mcp/handlers.test.ts` — invalid_argument en texte ; get_status ; get_views (en-tête + image + JSON, diffusion views, sans image) ; mouvements ok/erreur ; move_camera avec image ; report (manuel, falling, clôture différée par `after`)
- [ ] [TEST-10] `packages/server/src/mcp/createMcpServer.test.ts` (client du SDK en mémoire) — neuf outils listés avec descriptions et `required` ; blocs texte/image ; tool_call_start/result et block_activity ; limite d'appels puis report ; arguments malformés refusés par le SDK sans exception côté client
- [ ] [TEST-11] `packages/server/src/http/app.test.ts` — aller-retour MCP par `StreamableHTTPClientTransport` (listTools, callTool) ; 405 sur GET/DELETE ; /health, /episodes, /episodes/:id, 404
- [ ] [TEST-12] `packages/server/src/agentRunner.test.ts` — runner inerte journalise ; module absent → inerte ; module factice chargé et réveillé par la session
- [ ] [TEST-13] `packages/server/src/integration/episode.test.ts` (hub réel + sim scriptée sur WebSocket + MCP en mémoire + journal sur disque) — script `get_views → move_basket → move_scissors ×3 → open_scissors → cut → report` → `harvested`, journal écrit (8 appels, phases, résumés, vues) ; panier mal placé → `missed` ; coupe `misaligned` en texte
- [ ] [TEST-14] `packages/sim/src/bridge/bridge.test.ts` (fausse socket, faux timers) — hello puis state ; action_result + state ; views_result et repli sans image ; 5 Hz ; événements et republication ; reconnexion 2 s et arrêt par close ; `fakeBridge.test.ts` — ordre, vitesse, close

## Gates
- [ ] [GATE-1] npm run lint
- [ ] [GATE-2] npm run typecheck
- [ ] [GATE-3] npm test (89 tests attendus sans M1/M2/M3 : shared 18, sim 21, server 50)
- [ ] [GATE-4] npm run build
- [ ] [GATE-5] npm run shot → `data/shots/scene.png` toujours produit (la page se charge avec le bridge, sans erreur de page)
