# Étape 3 — M4 Perception : Canny + CLAHE, YOLO ONNX avec repli HSV, détecteur de réveil — Checklist

Issue : #4 · Plan : `docs/superpowers/plans/2026-09-17-etape-3-m4-perception.md` · Contrat : `docs/superpowers/plans/2026-09-17-etape-3-architecture.md` (section « Perception (M4) ») · Spec : sections 3, 4.4, 4.5, 8, 9, 10.

Tout le code M4 est dans `packages/sim/src/perception/` ; hors de ce dossier, seuls sont touchés `packages/sim/src/cameras/renderViews.ts` (`setEdgeFilter`/`getEdgeFilter`), `packages/sim/src/cameras/cameraModule.ts` (`renderCameraImage` par scène), `packages/sim/package.json` (deux dépendances WASM, versions exactes), `packages/sim/public/models/` (`tomato-ripe.json`, `.gitignore`), `scripts/export-yolo.py`, `packages/sim/src/App.tsx` (imports, `MODULES`, `<PerceptionBadge />`) et `packages/sim/tests/perception.spec.ts`. `packages/shared` n'est pas modifié. Le modèle ONNX est optionnel : aucun item ne dépend de sa présence sauf mention.

## Code
- [x] [SPEC-1] Dépendances `@techstark/opencv-js` 4.11.0-release.1 et `onnxruntime-web` 1.30.0 en versions exactes dans `dependencies` ; aucune dépendance native — `packages/sim/package.json`
- [x] [SPEC-2] `public/models/tomato-ripe.json` (`names: ["unripe", "ripe"]`, `imgsz: 640`, `opset: 12`, `source`), `.gitignore` (`*.onnx`, `*.pt`, `work/`), `scripts/export-yolo.py` (téléchargement de `best.pt` depuis le Space Hugging Face, `export(format='onnx', imgsz=640, opset=12)`, copie vers `tomato-ripe.onnx`, écriture du JSON) ; le `.onnx` exporté si Python le permet, sinon « YOLO non exporté » dans `plan_deviations` — `packages/sim/public/models/`, `scripts/export-yolo.py`
- [x] [SPEC-3] Types structurels : `RgbaImage`, `Detection` (`bbox [x, y, w, h]`, `score`, `label ripe|unripe`), `RipeDetector` (synchrone ou Promise), `PerceptionState`, `DETECTOR_INPUT_PX = 640` — `packages/sim/src/perception/types.ts`
- [x] [SPEC-4] `rgbToHsv` à l'échelle OpenCV (H 0..180, S et V 0..255) ; `resizeRgba` bilinéaire, alpha 255 — `packages/sim/src/perception/rgba.ts`
- [x] [SPEC-5] Érosion, dilatation, ouverture 3×3 sur masque binaire ; composantes connexes 4-connexité par pile explicite avec boîte et aire — `packages/sim/src/perception/morphology.ts`, `components.ts`
- [x] [SPEC-6] Détecteur HSV : masque rouge H < 10 ou H > 170, S > 100, V > 80 ; ouverture 3×3 ; aire ≥ 80 px ; score = aire / (w·h) ; label `ripe` ; trié par aire — `packages/sim/src/perception/hsvDetector.ts`
- [x] [SPEC-7] Letterbox 640 (CHW RGB 0..1, fond 114) et retour aux pixels source ; décodage YOLOv8 `[1, 4 + nc, N]` (cx, cy, w, h puis scores par classe) ; NMS gloutonne par classe IoU 0,45 ; `labelFromName` (`unripe` prioritaire) ; `parseModelMeta` — `packages/sim/src/perception/yoloDecode.ts`
- [x] [SPEC-8] `matchDetections` : détections `ripe` associées à la tomate projetée la plus proche si distance ≤ 0,75 × max(w, h), une tomate au plus (meilleur score), tri par score — `packages/sim/src/perception/matching.ts`
- [x] [SPEC-9] `createWakeGate(n = 5)` : identifiant renvoyé à la n-ième vue consécutive, remise à zéro sur changement ou `null`, `reset()` — `packages/sim/src/perception/wakeGate.ts`
- [x] [SPEC-10] Contours : interface `CvApi` structurelle, gris (`COLOR_RGBA2GRAY`) → `new cv.CLAHE(2, Size(8, 8))` → `Canny(50, 150, 3, false)` → blanc opaque sur transparent, chaque `Mat` et le CLAHE libérés même en cas d'erreur ; `createCannyClaheFilter(cv): EdgeFilter` ; `loadOpenCv()` par `import()` dynamique, `null` sans exception — `packages/sim/src/perception/cannyClahe.ts`
- [x] [SPEC-11] Module pur à dépendances injectées : tick toutes les 0,5 s sim (accumulation de `dtSimS`), jamais deux détections en parallèle ; YOLO si chargé et boîte `ripe` ≥ 0,4 sinon HSV ; association sur la projection `front` réduite ; garde-fou `state === 'ripe'` désactivable ; `ripe_detected { tomatoId, detector, confidence }` une seule fois par tomate ; réarmement sur `plant_regenerated` ; `state()`/`subscribe()` — `packages/sim/src/perception/perceptionRuntime.ts`
- [x] [SPEC-12] `setEdgeFilter`/`getEdgeFilter` dans `renderViews.ts`, pipeline lisant `getEdgeFilter()` à chaque rendu, Sobel par défaut ; `renderCameraImage(scene, camId)` dans `cameraModule.ts` (caméras indexées par `SceneHandle`, helpers masqués, `null` sans scène) — `packages/sim/src/cameras/renderViews.ts`, `cameraModule.ts`
- [x] [SPEC-13] `loadYoloDetector` : `onnxruntime-web/wasm`, `wasmPaths = { wasm: <asset ?url> }`, `numThreads = 1`, `executionProviders: ['wasm']`, fetch refusant le HTML de repli, tenseur `float32 [1, 3, 640, 640]`, décodage + NMS + unletterbox ; `null` et journal si le modèle, ses classes ou le runtime manquent — `packages/sim/src/perception/yoloDetector.ts`
- [x] [SPEC-14] `perceptionModule` (nom `perception`) câblé sur `renderCameraImage(ctx.scene, 'front')` réduit à 640, `hsvDetector`, `loadYoloDetector`, `loadOpenCv` + `createCannyClaheFilter`, `setEdgeFilter` ; exports `perceptionState`, `subscribePerception` ; `window.__tomatoPerception` ; `perceptionModule` en dernier dans `MODULES` (après `cameraModule`) — `packages/sim/src/perception/perceptionModule.ts`, `packages/sim/src/App.tsx`
- [x] [SPEC-15] `PerceptionBadge` (`data-testid="perception-badge"`) : état des contours, du modèle et du dernier tick, abonné à `subscribePerception`, monté dans l'`aside` avant `<AgentViews />` — `packages/sim/src/perception/PerceptionBadge.tsx`, `packages/sim/src/App.tsx`

## Tests
- [x] [TEST-1] `packages/sim/src/perception/rgba.test.ts` — rouge/vert/bleu → H 0/60/120 ; gris et noir ; `#c8261b` dans la bande rouge basse, vert loin ; rouge magenta > 170 ; redimensionnement uniforme, 800 → 640, identité
- [x] [TEST-2] `packages/sim/src/perception/components.test.ts` — érosion garde le centre d'un 3×3 ; dilatation d'un pixel au bord ; ouverture supprime le bruit ; trois composantes avec boîtes et aires ; pas de jonction diagonale ; image pleine 200×200 sans récursion
- [x] [TEST-3] `packages/sim/src/perception/hsvDetector.test.ts` — masque : rouge mûr accepté, vert/orange/fond rejetés, ombre sombre et rose délavé rejetés ; un disque rouge → une détection `ripe` avec boîte et taux de remplissage ≈ 0,78, bruit ignoré ; image sans rouge → rien ; tri par aire
- [x] [TEST-4] `packages/sim/src/perception/yoloDecode.test.ts` — letterbox 4×2 → 4×4 (bandes grises, plans CHW), 800 → 640 sans marge, retour des boîtes ; `labelFromName`, `parseModelMeta` ; tenseur synthétique `[1, 6, 5]` décodé, doublon supprimé par NMS, tri, dims invalides → vide ; `iou` et NMS inter-classes
- [x] [TEST-5] `packages/sim/src/perception/matching.test.ts` — association à la tomate proche, boîte lointaine ignorée ; meilleur score par tomate et tri ; `unripe` ignoré, sans tomate → vide ; tolérance proportionnelle à la boîte
- [x] [TEST-6] `packages/sim/src/perception/wakeGate.test.ts` — déclenchement à la n-ième vue, pas de doublon ; remise à zéro sur changement ou `null` ; défaut 5 et `reset()` ; n = 1
- [x] [TEST-7] `packages/sim/src/perception/cannyClahe.test.ts` — `edgesToWhiteRgba` ; ordre des appels (`matFromImageData`, `CLAHE(2, 8×8)`, `cvtColor(RGBA2GRAY)`, `apply`, `Canny(50, 150, 3, false)`) sur un faux `cv`, pixel de contour blanc, libération des 4 `Mat` et du CLAHE ; libération même si `Canny` lance
- [x] [TEST-8] `packages/sim/src/perception/perceptionRuntime.test.ts` — tick à 2 Hz sim et `ripe_detected` une seule fois après n vues, `detector: 'hsv'` ; garde-fou bloque `turning`, désactivé il laisse passer ; YOLO ≥ 0,4 préféré (`detector: 'yolo'`), < 0,4 → HSV ; réarmement sur `plant_regenerated` et `setEdgeFilter` appelé quand OpenCV est prêt ; aucun tick sans image, jamais deux détections en parallèle ; abonnés notifiés
- [x] [TEST-9] `packages/sim/tests/perception.spec.ts` (Playwright) — `opencvReady` en < 30 s, `renderViews(['front'])` écrit `data/shots/view-front-canny.png`, pastille « Canny », `ripen_next` (jusqu'à 3) puis `ripe_detected` reçu via `runtime.onEvent`, `data/shots/perception.json` et `perception.png` écrits, aucune erreur de page

## Gates
- [x] [GATE-1] npm run lint
- [x] [GATE-2] npm run typecheck
- [x] [GATE-3] npm test (44 tests M4 en plus des existants)
- [x] [GATE-4] npm run build (avertissements attendus : `fs`/`path`/`crypto` externalisés pour `opencv.js`, chunk `opencv` ≈ 11 Mo)
- [x] [GATE-5] npm run shot → `data/shots/view-front-canny.png`, `perception.png`, `perception.json` produits (en plus des captures M1-M3)

## Critères visuels (visual-checker, sur `data/shots/view-front-canny.png`, `data/shots/perception.png` et `data/shots/perception.json`)
- [ ] [VIS-1] `view-front-canny.png` : les contours du plant, des tomates, de la tige et du panier sont des traits blancs **fins d'un pixel, continus et fermés** (Canny), sans le halo épais ni les zones pleines des contours Sobel de `view-front.png` (M3) ; le fond reste assombri, les couches d'annotation M3 (grille, bandeau, ciseaux magenta, panier jaune) sont intactes par-dessus
- [ ] [VIS-2] `view-front-canny.png` : les contours sont présents aussi dans les zones sombres et peu contrastées de l'image (feuilles à l'ombre, tige inférieure) — effet du CLAHE — sans bruit de fond (pas de pointillé blanc sur le sol ou le fond uniforme)
- [ ] [VIS-3] `perception.png` : en haut du panneau droit, trois pastilles monospace lisibles : « contours Canny + CLAHE », « YOLOv8 ONNX + HSV » ou « HSV seul (YOLO absent) », « dernier tick : YOLO|HSV · n boîte(s) » ; la pastille du dernier tick est mise en évidence (fond magenta sombre) quand YOLO a déclenché
- [ ] [VIS-4] `perception.json` : `opencvReady: true` ; si `ripenOk: true`, `events[0].type === 'ripe_detected'` avec `detector` ∈ {`yolo`, `hsv`} et `confidence` > 0,4 ; `lastDetections` non vide avec des boîtes de 20 à 120 px dans une image de 640 px ; `yoloReady` cohérent avec la présence de `packages/sim/public/models/tomato-ripe.onnx`
- [ ] [VIS-5] `perception.png` : la vue spectateur montre au moins une tomate rouge (mûrie par le test) et les vues M3 restent affichées ; aucune erreur visible, aucun panneau vide
