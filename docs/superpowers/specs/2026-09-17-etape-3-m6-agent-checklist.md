# Étape 3 — M6 Runner d'agent : Claude Agent SDK, prompt système, réveil, streaming, limite d'appels — Checklist

Issue : #6 · Plan : `docs/superpowers/plans/2026-09-17-etape-3-m6-agent.md` · Contrat : `docs/superpowers/plans/2026-09-17-etape-3-architecture.md` (section « Runner d'agent (M6) » ; « Serveur (M5) » pour `Hub`, `Session`, `SimBridge`) · Spec : sections 1, 3, 4.5, 5, 8, 9, 11.

Périmètre des fichiers : `packages/server/src/agent/**`, `packages/server/prompts/**`, `packages/server/scripts/wake.ts`, `packages/server/package.json`, `packages/server/tsconfig.json` (`include` + `scripts`), `packages/server/src/index.ts` (branchement seulement). **Aucun fichier dans `packages/shared/`.** Aucune dépendance native (le binaire Claude Code est embarqué par le SDK via une dépendance optionnelle npm).

## Code
- [x] [SPEC-1] Dépendances `@anthropic-ai/claude-agent-sdk` (^0.3.275), `ws` ; devDeps `@types/ws`, `tsx` ; script `wake` = `tsx scripts/wake.ts` ; `tsconfig.json` inclut `scripts` — `packages/server/package.json`, `packages/server/tsconfig.json`
- [x] [SPEC-2] Prompt système complet en anglais : rôle et outils seuls, repère monde (X droite, Y arrière, Z haut, cm et degrés), les trois vues orthographiques et leurs axes, glossaire des étiquettes françaises des overlays, les cinq couches (fond assombri + contours, grille et échelle, marqueurs numérotés et badge occultée, tige cible cyan, schéma des ciseaux avec `lame` et `normale`, panier jaune, verticale de chute et impact, bandeau), géométrie des ciseaux (orientation de base, lacet/tangage/roulis) et règle de coupe (0,6 cm, 45°), obstacles et portée, panier sous l'impact, procédure en boucle fermée (regarder, panier, approche 5 cm puis 1 cm, vérification dans la vue où la tige est dans le plan puis dans les deux autres, ouvrir, couper, `get_status` jusqu'à `tomato_landed`, `report`), codes d'erreur et conduite à tenir, limite de 40 appels, raisonnement court avant chaque action, mémoire non fiable entre épisodes — `packages/server/prompts/system.md`
- [x] [SPEC-3] Chargement du prompt depuis `prompts/system.md` relatif au module (`loadSystemPrompt`) — `packages/server/src/agent/systemPrompt.ts`
- [x] [SPEC-4] Types du module : `WakeEvent { tomatoId; positionCm; ripeness }`, interfaces structurelles `AgentHub`, `AgentSession` (`get`, `startEpisode`, `endEpisode`, `onPhase`), `AgentSim` (`latestState`), `AgentMessage` (variantes `system`/`init`, `assistant`, `stream_event`, `result`, `other`), `QueryFn` injectable, `AgentRunner { wake; busy; stop; whenIdle }` — `packages/server/src/agent/types.ts`
- [x] [SPEC-5] Adaptateur `sdkQuery` sur `query()` du SDK ; `toAgentMessage(m: SDKMessage)` renvoie les messages tels quels dans chaque branche (vérification de forme par le compilateur, sans `any` ni cast) — `packages/server/src/agent/sdkQuery.ts`
- [x] [SPEC-6] Réduction pure du flux : `agent_text` par bloc de texte complet et non vide (trim) ; `session_id`, modèle et statut du serveur `robot` depuis `init` ; comptage des `tool_use` `mcp__robot__*` ; détection d'un `report` valide (`ToolSchemas.report`) avec `outcome` et `note` ; coût, durée, tours, `is_error`, `subtype` depuis `result` ; `text_delta` renvoyé à part (console) et jamais diffusé ; `episodeOutcome` (report, sinon `aborted` avec la raison) ; `episodeEndMessage` (`episode_end` avec `toolCalls`, `costUsd`, `durationMs`, durée mesurée en repli) — `packages/server/src/agent/streamToDashboard.ts`
- [x] [SPEC-7] Message de réveil : tomate cible avec position en cm (une décimale) et ripeness, texte d'état, consigne (panier, approche 5 cm puis 1 cm, aligner, ouvrir, couper, `get_status`, `report`), limite `MAX_TOOL_CALLS_PER_EPISODE`, « Start with get_views » ; avertissement « positions périmées » quand la session est reprise ; `summarizeWorld` (temps sim, tomates et mûres, ciseaux, panier ; « no simulation state » si null) — `packages/server/src/agent/wakePrompt.ts`
- [x] [SPEC-8] Options de `query()` : `systemPrompt` (chaîne), `model`, `mcpServers: { robot: { type: 'http', url, alwaysLoad: true } }`, `tools: []`, `allowedTools: ['mcp__robot__*']`, `permissionMode: 'dontAsk'`, `strictMcpConfig: true`, `settingSources: []`, `persistSession: true`, `includePartialMessages: true`, `maxTurns: 50`, `abortController`, `env: { ...process.env, MAX_MCP_OUTPUT_TOKENS: '400000' }`, `resume` seulement si une session est connue, `stderr` seulement si fourni ; `DEFAULT_MODEL = 'claude-opus-5'` — `packages/server/src/agent/queryOptions.ts`
- [x] [SPEC-9] Runner : file de réveils, un épisode à la fois, doublons (tomate en cours ou en file) ignorés ; `startEpisode` seulement si `session.get().episodeId === null` ; `episodeId` lu dans la session (`'manual'` sinon) ; `episode_start` avec `sessionResumed` ; `query()` avec reprise à partir du deuxième épisode ; `agent_text` diffusés au fil de l'eau, deltas vers `onDelta` ; `session_id` mémorisé, oublié si la reprise a échoué ; `episode_end` diffusé puis `endEpisode(outcome, note)` si l'épisode est encore ouvert (`aborted` + « agent ended without report » sans `report`) ; erreurs de `query()` attrapées (épisode `aborted`, jamais de crash) ; `stop()` = abort + file vidée + réveils refusés ; `whenIdle()` — `packages/server/src/agent/agentRunner.ts`
- [x] [SPEC-10] Serveur de réveil manuel `node:http` sur `127.0.0.1:7333` (`TOMATO_WAKE_PORT`, 0 = port libre) : `POST /wake/<id>` → 202 `{ queued, tomatoId, behindRunningEpisode }`, 404 `unknown_tomato` avec les identifiants connus ; `GET /wake` → `{ busy, tomatoes }` ; 404 ailleurs — `packages/server/src/agent/wakeServer.ts`
- [x] [SPEC-11] `startAgent` : prompt chargé une fois, runner créé avec `statusText = summarizeWorld(sim.latestState())`, abonnement `session.onPhase` → sur `detected`, réveil avec `targetTomatoId` résolu dans l'état de la sim (`resolveWakeEvent`), serveur de réveil, `agentEnv` (`TOMATO_MODEL` défaut `claude-opus-5`, `TOMATO_WAKE_PORT` défaut 7333, `TOMATO_AGENT` `off` désactive), `close()` = `stop` + fermeture du serveur — `packages/server/src/agent/startAgent.ts`
- [x] [SPEC-12] Branchement : `index.ts` appelle `startAgent({ hub, session, sim, mcpUrl })` quand `agentEnv().enabled`, ferme l'agent à l'arrêt ; le runner no-op de M5 est remplacé. Si M5 n'est pas mergé au moment de la PR : non fait, déviation notée et issue `ad-hoc` ouverte (voir plan Task 8) — `packages/server/src/index.ts`
  - M5 n'est pas mergé et possède `src/index.ts` : **cas (b)**, `index.ts` non touché. À la place, M6 expose `createAgentRunner(deps)` dans `packages/server/src/agent/index.ts`, la forme exacte que `loadAgentRunner` de M5 charge dynamiquement (`AGENT_MODULE = './agent/index.js'`). Issue `ad-hoc` ouverte pour le branchement final (`sim` dans les deps, fermeture à l'arrêt).
- [x] [SPEC-13] Script `wake` : `npm run wake -w @tomato/server -- <tomatoId>` se connecte au hub WebSocket (`hello { role: 'dashboard' }`, port `TOMATO_WS_PORT` 7332), déclenche `POST /wake/<id>`, imprime `episode_start`, `agent_text`, `tool_call_start`, `tool_call_result`, `phase`, `sim_event`, `episode_end`, écrit chaque ligne horodatée dans `data/episodes/wake-<id>-<horodatage>.log`, sort avec 0 si `harvested`, 1 sinon, 2 sans argument, délai 20 min — `packages/server/src/agent/wakeCli.ts`, `packages/server/scripts/wake.ts`
- [x] [SPEC-14] Hypothèses M5 vérifiées **sur le plan `docs/superpowers/plans/2026-09-17-etape-3-m5-serveur.md`** (M5 non mergé : vérification sur le code reportée à l'intégration) et écarts notés dans `plan_deviations` : `startEpisode` pose `episodeId` et la phase `detected` ; `endEpisode` clôt le journal ; `report` de M5 ne diffuse pas `episode_end` (le runner est le seul émetteur) ; `tool_call_*` diffusés par le MCP seulement

## Tests
- [x] [TEST-3] `packages/server/src/agent/systemPrompt.test.ts` — prompt > 6000 caractères, nomme les neuf outils, repère et unités, trois vues, glossaire français, 0,6 cm, 45°, pas de 5 puis 1 cm, limite 40, consigne de raisonnement
- [x] [TEST-6] `packages/server/src/agent/streamToDashboard.test.ts` — `robotToolName` ; `init` (session, modèle, statut MCP) ; un `agent_text` par bloc non vide, trimé ; deltas à part ; comptage des outils robot seulement ; `report` valide détecté, invalide ignoré ; extraction du `result` ; `episodeOutcome` (report / `error_max_turns` / erreur levée / sans report) ; `episodeEndMessage` avec repli de durée et coût 0
- [x] [TEST-7] `packages/server/src/agent/wakePrompt.test.ts` — `summarizeWorld` (null, temps, tomates mûres, ciseaux, panier) ; `buildWakePrompt` (cible en cm, statut, limite, `get_views`, avertissement de reprise)
- [x] [TEST-8] `packages/server/src/agent/queryOptions.test.ts` — serveur MCP `robot` HTTP avec `alwaysLoad`, `allowedTools`, `tools: []`, `dontAsk`, `strictMcpConfig`, `settingSources: []` ; prompt, modèle, streaming, `maxTurns`, `persistSession`, `abortController` ; `env` avec `MAX_MCP_OUTPUT_TOKENS` et `PATH` hérité ; `resume` et `stderr` conditionnels ; `tsc` vérifie les noms contre `Options` du SDK
- [x] [TEST-9] `packages/server/src/agent/agentRunner.test.ts` (`query` factice, sans réseau) — épisode nominal (`episode_start` → `agent_text` → `episode_end`, `startEpisode`/`endEpisode`, prompt) ; reprise de session au deuxième épisode ; file et doublons ; `aborted` sans `report` ; `query` qui lève → `aborted`, reprise abandonnée ; `stop()` (abort, file vidée, réveils refusés)
- [x] [TEST-10] `packages/server/src/agent/wakeServer.test.ts` — 202 et `behindRunningEpisode`, 404 tomate inconnue avec identifiants, `GET /wake`, 404 ailleurs (port 0, `fetch` réel)
- [x] [TEST-11] `packages/server/src/agent/startAgent.test.ts` — `resolveWakeEvent`, `agentEnv` ; phase `detected` → épisode avec la cible et le résumé de la sim ; `POST /wake/<id>` → second épisode ; `close()`
- [x] [TEST-13] `packages/server/src/agent/wakeCli.test.ts` — `formatTraceLine` pour chaque type utile et null pour les autres ; `EPISODES_DIR` et `transcriptPath` sous `data/episodes/` à la racine

## Gates
- [x] [GATE-1] npm run lint — exit 0
- [x] [GATE-2] npm run typecheck — exit 0 (shared, server, sim)
- [x] [GATE-3] npm test — exit 0, 48 fichiers / 265 tests, dont 41 tests M6 sur 9 fichiers `src/agent/*.test.ts`
- [x] [GATE-4] npm run build — exit 0
- [ ] [GATE-5] **à faire après merge de M5** (exception acceptée par l'orchestrateur : impossible sans le hub, le MCP et l'`index.ts` de M5) Vérification manuelle (plan Task 10) : serveur + sim démarrés, `npm run wake -w @tomato/server -- <tomatoId>` produit un épisode réel visible dans la trace du dashboard (ou la console du bridge si M7 n'est pas mergé), le serveur affiche `robot MCP connected`, et le transcript est conservé — preuve : chemin `data/episodes/wake-<id>-<horodatage>.log` cité dans la PR, avec l'issue (`harvested` / `missed` / `aborted`), le nombre d'appels et le coût. Un second réveil montre `(resumed session)`. Si M5 n'est pas mergé : item laissé décoché, PR ouverte en l'expliquant.
