# Étape 3 — M7 Dashboard : panneaux, trace, statuts, schéma bloc animé, contrôles de tournage, replay — Checklist

Issue : #7 · Plan : `docs/superpowers/plans/2026-09-17-etape-3-m7-dashboard.md` · Contrat : `docs/superpowers/plans/2026-09-17-etape-3-architecture.md` (section « Dashboard (M7) », « Pont côté sim (M5) », « Perception (M4) ») · Spec : sections 1, 3, 6, 8, 9, 10.

Tout le code M7 est dans `packages/sim/src/dashboard/`. Hors de ce dossier, M7 touche seulement : `packages/sim/src/App.tsx` (réécriture de la mise en page, runtime, `MODULES` et pont conservés), `packages/sim/src/styles.css` (tokens de palette), `packages/sim/package.json` (devDependencies `@testing-library/react`, `@testing-library/dom`, `jsdom`), `packages/sim/vitest.config.ts` (motif `*.test.{ts,tsx}`), `packages/sim/tests/dashboard.spec.ts`, et `packages/sim/src/bridge/fakeBridge.ts` **uniquement s'il n'existe pas encore** (contrat M5, signature exacte du document d'architecture). `packages/shared` n'est pas modifié. `packages/sim/src/cameras/AgentViews.tsx` (M3) est réutilisé, pas réécrit.

## Code
- [x] [SPEC-1] Types du store (`DashboardState`, `TraceEntry` = `{ kind: 'text' | 'tool' | 'event' | 'phase'; atMs; title; detail?; ok?; durationMs? }` + `id`/`callId`, `LocalMessage`) et contrat de pont côté dashboard (`DashboardBridge`, `ScriptEntry`) — `packages/sim/src/dashboard/dashboardTypes.ts`, `bridgeTypes.ts`
- [x] [SPEC-2] Mise en forme française de la trace : un titre d'une ligne par outil avec ses arguments en clair (ciseaux, panier, caméra, vues, coupe, rapport…), titres des événements sim, des phases, des épisodes ; durées (ms / s / min), coût (4 décimales, `$`), nombres à virgule et signe moins typographique, heure HH:MM:SS — `packages/sim/src/dashboard/traceFormat.ts`
- [x] [SPEC-3] Réducteur pur des messages serveur : chaque type de `ServerToDashboard` met à jour le bon champ (`snapshot` → phase/épisode/horloge, `phase`, `episode_start`, `episode_end` → compteurs + coût cumulé, `agent_text`, `tool_call_start` → entrée en attente, `tool_call_result` → complète l'entrée par `callId` (ok, durée, résumé), `views` → images par caméra + `lastViewsAt`, `sim_event`, `block_activity` → bloc/flèche + horodatage) ; trace plus récent en tête, plafonnée à 200 — `packages/sim/src/dashboard/reduceServer.ts`
- [x] [SPEC-4] Messages locaux : connexion, `local_reset` (début de replay, garde interface/modèle/connexion), modèle, bascules contrôles / mode agent / schéma, vue agrandie — `packages/sim/src/dashboard/reduceLocal.ts`, `initialState.ts`
- [x] [SPEC-5] `createDashboardStore()` : `get` / `dispatch(message, nowMs?)` / `subscribe`, snapshot immuable, abonnés notifiés seulement sur changement réel (compatible `useSyncExternalStore`) — `packages/sim/src/dashboard/dashboardStore.ts`
- [x] [SPEC-6] `createFakeBridge(script: { atMs; message: ServerToDashboard }[]) → Bridge` avec `Bridge { onServerMessage; status(); onStatus; close() }` (contrat Étape 3), créé seulement si `packages/sim/src/bridge/fakeBridge.ts` n'existe pas — `packages/sim/src/bridge/fakeBridge.ts`
- [x] [SPEC-7] `createBridgeSlot(store)` : un seul pont écouté à la fois (réel M5 ou replay), `play` remet la trace à zéro et ignore le pont réel pendant le replay, `stop` rebranche le pont réel — `packages/sim/src/dashboard/bridgeSlot.ts`
- [x] [SPEC-8] Gardes de merge : pont M5 chargé par `import.meta.glob('../bridge/createBridge.ts')` (null si absent, sans casser le build) ; `perceptionState()` de M4 lu par glob avec repli « HSV/Sobel » dans le bandeau — `packages/sim/src/dashboard/bridgeLoader.ts`, `perceptionInfo.ts`
- [x] [SPEC-9] API des épisodes : `GET http://localhost:7331/episodes`, `GET /episodes/:id` validé, `toScript(file, speed)` (tri, base 0, facteur de vitesse), modèle via `GET /health` si exposé — `packages/sim/src/dashboard/episodesApi.ts`
- [x] [SPEC-10] Scénario de démo scripté sans serveur ni Claude (réveil → vues → panier → collision surlignée → coupe → chute → rapport → `episode_end` récoltée → repos), pur et rejouable par le pont simulé — `packages/sim/src/dashboard/demoScript.ts`
- [x] [SPEC-11] Palette de la spec (rouge mûr, vert immature, orange transition, cyan tige, magenta ciseaux, jaune panier, blanc, gris axes) en tokens `@theme` Tailwind 4, thème sombre, nombres en monospace `tabular-nums`, flash CSS du cadre des vues avec `prefers-reduced-motion`, aucun élément décoratif — `packages/sim/src/styles.css`, `packages/sim/src/dashboard/ui.ts`
- [x] [SPEC-12] `StatusBar` : pastilles des huit phases (l'active seule colorée, `aria-current="step"`), compteurs récoltées / ratées, temps sim et facteur (horloge de la sim locale via `useWorldClock`, sinon celle du snapshot, « pause »), détecteur, modèle, coût cumulé, état de connexion (connecté / hors ligne / replay) — `packages/sim/src/dashboard/StatusBar.tsx`, `useWorldClock.ts`
- [x] [SPEC-13] `TracePanel` : plus récent en haut, heure, nature (agent / outil / événement / phase), titre, détail, durée ; erreurs surlignées (`data-ok="false"`, bordure et fond rouge mûr) — `packages/sim/src/dashboard/TracePanel.tsx`, `TraceRow.tsx`
- [x] [SPEC-14] `BlockDiagram` : SVG inline Simulation → Perception → Serveur MCP → Agent → Dashboard sur un bus d'événements ; sur `block_activity`, bloc cible, segment de bus et flèche allumés 600 ms avec le libellé ; repliable (bouton `aria-expanded`, touche `b`) — `packages/sim/src/dashboard/BlockDiagram.tsx`, `blockLayout.ts`, `useFlash.ts`
- [x] [SPEC-15] `Controls` : pause / reprendre, vitesse ×1 ×2 ×5 ×10, « Mûrir la prochaine tomate » (`ripen_next`), « Nouveau plant » (`new_plant`) via `runtime.apply` ; « Masquer les contrôles (h) » ; « Ce que voit l'agent (v) » ; tous les boutons ont un nom accessible et `aria-pressed` pour les bascules — `packages/sim/src/dashboard/Controls.tsx`
- [x] [SPEC-16] `ReplayPanel` : liste des épisodes, choix de la vitesse, « Rejouer » par `createFakeBridge` à la vitesse choisie, « Arrêter », état affiché (serveur injoignable dit tel quel) — `packages/sim/src/dashboard/ReplayPanel.tsx`
- [x] [SPEC-17] `ViewsPanel` : les trois vues telles que reçues (`views`) avec le nom de la caméra et ses axes, clic pour agrandir (dialogue, Échap), flash bref à chaque `views` ; sans pont, repli sur `AgentViews` de M3 (bouton « Rafraîchir les vues » conservé) — `packages/sim/src/dashboard/ViewsPanel.tsx`
- [x] [SPEC-18] `Dashboard` : bandeau haut, gauche 55 % `SpectatorView`, droite haut vues, droite bas trace, bandeau bas repliable ; mode « ce que voit l'agent » : 30 / 70 (vues agrandies, 3D réduite) ; touches `h`, `v`, `b` ignorées dans les champs de saisie ; `SpectatorView` jamais remonté par un changement de mise en page — `packages/sim/src/dashboard/Dashboard.tsx`
- [x] [SPEC-19] `App.tsx` réécrit : runtime et `MODULES` conservés, `window.__tomato = { runtime, renderViews? }` plus, en dev seulement, `fakeBridge`, `attachBridge`, `stopReplay`, `demoScript` ; pont M5 chargé s'il existe (`slot.setLive`) ; modèle demandé à `/health` ; `data-testid="spectator"` conservé — `packages/sim/src/App.tsx`

## Tests
- [x] [TEST-1] `packages/sim/src/dashboard/traceFormat.test.ts` — nombres, durées, coût, heure, première ligne ; titres des neuf outils avec arguments ; événements, phases, épisodes, vues
- [x] [TEST-2] `packages/sim/src/dashboard/dashboardStore.test.ts` — un cas par type de `ServerToDashboard` (dix), rapprochement `tool_call_start` / `tool_call_result`, ordre plus récent en tête et plafond 200, messages locaux, `local_reset`, notification du store
- [x] [TEST-3] `packages/sim/src/bridge/fakeBridge.test.ts` — messages livrés à `atMs` dans l'ordre, `close` annule et notifie une fois, désabonnement
- [x] [TEST-4] `packages/sim/src/dashboard/bridgeSlot.test.ts` — compatibilité structurelle `createFakeBridge` → `DashboardBridge`, pont réel, replay qui ignore le pont réel puis `stop` qui le rebranche, état hors ligne
- [x] [TEST-5] `packages/sim/src/dashboard/bridgeLoader.test.ts`, `perceptionInfo.test.ts` — null sans module M5 / M4, appel de `createBridge` avec les options, lecture de `perceptionState`, libellés du détecteur avec repli « HSV/Sobel »
- [x] [TEST-6] `packages/sim/src/dashboard/episodesApi.test.ts` — `toScript` (tri, base 0, vitesse), `isEpisodeFile`, `listEpisodes` / `loadEpisode` / `fetchServerModel` avec `fetch` simulé
- [x] [TEST-7] `packages/sim/src/dashboard/demoScript.test.ts` — ordre temporel, phases jusqu'à `harvested` puis `idle`, `views` seulement si fournies, chaque appel a son résultat, une seule erreur surlignée une fois rejoué dans le store
- [x] [TEST-8] `packages/sim/src/dashboard/blockLayout.test.ts` — cinq blocs dans l'ordre, sans chevauchement, segment de bus ordonné
- [x] [TEST-9] `packages/sim/src/dashboard/StatusBar.test.tsx` (jsdom, testing-library) — huit pastilles en français, `aria-current` sur la phase courante, compteurs, coût, modèle, détecteur, horloge locale et « pause »
- [x] [TEST-10] `packages/sim/src/dashboard/TracePanel.test.tsx` (jsdom, testing-library) — ordre plus récent en haut, heure, durée, `data-kind`, erreur avec `data-ok="false"` et détail, trace vide
- [x] [TEST-11] `packages/sim/tests/dashboard.spec.ts` (Playwright) — la page charge, `window.__tomato.fakeBridge` / `attachBridge` / `demoScript` injectent l'épisode scripté, la trace affiche « Épisode terminé : récoltée » en tête et une erreur surlignée, compteur 1, coût `0,0421 $`, connexion « replay », `dashboard.png` à 1920×1080 puis `v` + `h` → `dashboard-agentview.png` ; `scene.spec.ts`, `views.spec.ts` et `robot.spec.ts` restent verts (`data-testid="spectator"`, bouton « Rafraîchir les vues », `img[alt="vue front"]`)

## Gates
- [x] [GATE-1] npm run lint
- [x] [GATE-2] npm run typecheck
- [x] [GATE-3] npm test
- [x] [GATE-4] npm run build
- [x] [GATE-5] npm run shot → `data/shots/dashboard.png` et `data/shots/dashboard-agentview.png` produits (en plus de `scene.png`, `view-*.png`, `robot.png`)

## Critères visuels (visual-checker, sur `data/shots/dashboard.png` et `data/shots/dashboard-agentview.png`, spec section 6)
- [ ] [VIS-1] Disposition 55 / 45 : à gauche la vue spectateur 3D (plant, bras, panier, frustums des caméras) occupe environ 55 % de la largeur ; à droite les vues en haut et la trace en bas ; bandeau de statuts en haut, schéma bloc en bas ; rien ne déborde à 1920×1080
- [ ] [VIS-2] Trois vues annotées côte à côte à droite (top, front, side), chacune avec son nom et ses axes ; images non noires (fond assombri, contours blancs, grille, bandeau) telles que l'agent les reçoit
- [ ] [VIS-3] Trace de l'agent : la ligne la plus récente est en haut (« Épisode terminé : récoltée… ») ; les textes de l'agent, les appels d'outils en français avec leurs arguments (« Ciseaux → X 8, Y −2, Z 41 », « Panier → X 12, Y −3 »), les résultats et les durées sont lisibles ; l'appel en erreur (collision) est surligné en rouge, les autres ne le sont pas
- [ ] [VIS-4] Pastilles de phase : les huit phases en français dans le bandeau haut, une seule remplie (couleur de la phase), les autres en gris
- [ ] [VIS-5] Compteurs « récoltées 1 », « ratées 0 » dans le bandeau haut, chiffres en monospace
- [ ] [VIS-6] Temps sim (« t sim 12,3 s ») et facteur (« ×1 » ou « pause ») dans le bandeau haut, en monospace
- [ ] [VIS-7] Modèle (« claude-opus-5 » ou celui annoncé par le serveur) et coût cumulé (« 0,0421 $ ») dans le bandeau haut, à droite ; état de connexion « replay » avec sa pastille jaune
- [ ] [VIS-8] Schéma bloc en bas : cinq blocs Simulation, Perception, Serveur MCP, Agent, Dashboard reliés à un bus ; dans `dashboard.png`, le bloc Dashboard est allumé en cyan avec sa flèche et le libellé « episode_end » sur le bus, ou, si la capture est tombée après les 600 ms, l'en-tête du schéma affiche « server → dashboard : episode_end »
- [ ] [VIS-9] Contrôles de tournage : dans `dashboard.png`, la barre (Pause, ×1 ×2 ×5 ×10, Mûrir la prochaine tomate, Nouveau plant, Replay, Ce que voit l'agent (v), Masquer les contrôles (h)) est visible en bas de la vue 3D ; dans `dashboard-agentview.png` (après `h`) elle a disparu et rien ne recouvre la vue 3D
- [ ] [VIS-10] Mode « ce que voit l'agent » (`dashboard-agentview.png`, après `v`) : les trois vues sont nettement plus grandes (colonne droite ≈ 70 %), la vue 3D réduite à ≈ 30 %, la trace toujours lisible en dessous des vues
- [ ] [VIS-11] Palette : fond sombre uniforme, texte clair ; seules les informations sont colorées (pastille de phase, erreur en rouge mûr, bloc actif en cyan, pastille de connexion) ; aucun dégradé, aucune ombre décorative, aucune icône ornementale
- [ ] [VIS-12] Lisibilité à 1080p : tous les textes du bandeau, de la trace et du schéma sont lisibles à 100 % (≥ 11 px, contraste net) ; les nombres sont en monospace et alignés ; aucun texte tronqué ni superposé
