*.onnx
*.pt
work/
# Exception : le modèle affiné sur les rendus de la sim est livré avec le dépôt (12,3 Mo), pour que
# `npm run demo` détecte avec le modèle sans passer par l'étape Python (issue #36).
!tomato-ripe.onnx
